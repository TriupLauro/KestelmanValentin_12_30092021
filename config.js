// Configuration for the app

/**
 * If set to false, the app will get the data from the mockedData.js file
 * If set to true, the app will get the data by calling the api - The api must be active at the same time as this app
 * The repository for the API must be installed separately - instructions included in the README
 * @type {boolean}
 */
export const callTheAPI = true